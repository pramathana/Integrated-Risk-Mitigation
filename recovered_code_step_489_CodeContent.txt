"import json\nimport re\n\ntranscript_path = r\"C:\\Users\\Jesano Pramathana\\.gemini\\antigravity-ide\\brain\\e925896e-7954-4df4-ae9a-65e4ad27b524\\.system_generated\\logs\\transcript.jsonl\"\n\n# We want to find the latest full content of index.html or code edits.\n# Let's search from the end of the file backwards to find the last step that contains a large chunk of index.html.\n\nwith open(transcript_path, \"r\", encoding=\"utf-8\") as f:\n    lines = f.readlines()\n\nprint(f\"Total lines in transcript: {len(lines)}\")\n\n# Let's search backwards\nfor i in range(len(lines) - 1, -1, -1):\n    line = lines[i]\n    if \"index.html\" in line:\n        try:\n            data = json.loads(line)\n            step_index = data.get(\"step_index\")\n            step_type = data.get(\"type\")\n            content = data.get(\"content\", \"\")\n            \n            # Check if this contains code content of index.html\n            # Let's see if there is code content in the tool_calls or if the step content itself has it\n            tool_calls = data.get(\"tool_calls\", [])\n            for tc in tool_calls:\n                args = tc.get(\"arguments\", {}) if tc.get(\"arguments\") else {}\n                if \"CodeContent\" in args:\n                    code = args[\"CodeContent\"]\n                    if \"const embeddedMarkdown =\" in code or \"marked.parse\" in code:\n                        print(f\"Found CodeContent in step {step_index} ({step_type})! Length: {len(code)}\")\n                        # Save this code content as index.html\n                        with open(\"recovered_index.html\", \"w\", encoding=\"utf-8\") as out:\n                            out.write(code)\n                        print(\"Saved to recovered_index.html!\")\n                        import sys\n                        sys.exit(0)\n                        \n                if \"ReplacementContent\" in args:\n                    repl = args[\"ReplacementContent\"]\n                    print(f\"Found ReplacementContent in step {step
<truncated 540 bytes>