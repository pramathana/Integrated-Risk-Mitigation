"Replace the `fetchAndRenderMarkdown` function with a synchronous `renderMarkdown` function that reads from embedded `<script type=\"text/markdown\">` tags. Inject the markdown content into these script tags right above the main `<script>` block."