[{"AllowMultiple":false,"EndLine":1584,"ReplacementContent":"    function openLightbox(src) {\n      document.getElementById('lightbox-img').src = src;\n      document.getElementById('lightbox').style.display = 'flex';\n    }\n    function closeLightbox() {\n      document.getElementById('lightbox').style.display = 'none';\n    }\n\n    // View Switching\n    function switchView(viewId) {\n      document.getElementById('view-criteria').style.display = 'none';\n      document.getElementById('view-assessment').style.display = 'none';\n      document.getElementById('view-priority').style.display = 'none';\n      document.getElementById('view-dashboard').style.display = 'none';\n      \n      document.getElementById(viewId).style.display = 'block';\n      \n      document.querySelectorAll('.menu-btn').forEach(btn => {\n        btn.classList.remove('active');\n        if (btn.getAttribute('data-view') === viewId) {\n          btn.classList.add('active');\n        }\n      });\n      \n      // Resize charts if switching to dashboard\n      if (viewId === 'view-dashboard') {\n        if (riskChartInstance) riskChartInstance.resize();\n        if (layerChartInstance) layerChartInstance.resize();\n        if (sankeyChartInstance) sankeyChartInstance.resize();\n      }\n    }\n\n    // Fetch and Parse Markdown\n    async function fetchAndRenderMarkdown() {\n      try {\n        const criteriaRes = await fetch('KRITERIA SKALA RISIKO.md');\n        if (criteriaRes.ok) {\n          const text = await criteriaRes.text();\n          document.getElementById('md-criteria').innerHTML = marked.parse(text);\n        } else {\n          document.getElementById('md-criteria').innerHTML = '<p>Error loading content.</p>';\n        }\n\n        const assessmentRes = await fetch('IT RISK ASSESSMENT.md');\n        if (assessmentRes.ok) {\n          const text = await assessmentRes.text();\n          document.getElementById('md-assessment').innerHTML = marked.parse(text);\n        } else {\n          document.getElementById('md-assessment'
<truncated 4334 bytes>